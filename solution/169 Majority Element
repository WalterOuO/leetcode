def majorityElement(self, nums: List[int]) -> int:
      ## Method1: .get() to get key
      dic = {}
      for i in nums:
          dic[i] = dic.get(i, 0) +  1    #.get取出key值
          if dic.get(i, ) > len(nums)/2:
              return i

      ## Method2: Two lisst
      list1 = []
      list2 = []
      nums.sort()
      for i in nums:
          if dic.get(i, ) > len(nums)/2:
              return i
